M2h: engine-built display name — SetName then the engine's own display-name builder reconciles the record (standing rule: follow engine flows). Test: equip fresh weapon, drop, hover the rollover.
built from native run 28834996227
